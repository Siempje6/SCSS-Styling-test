{
  "devDependencies": {
    "sass": "^1.93.2"
  },
  "scripts": {
    "watch:sass": "sass --watch scss/style.scss:css/style.min.css --style=compressed",
    "build:sass": "sass --watch scss/style.scss:css/style.css"
  }
}